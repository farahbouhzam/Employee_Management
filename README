Employee Management is a full-stack application designed to manage employee data efficiently.
It provides a RESTful backend API, a frontend user interface, and an OpenAPI specification to support CRUD operations and future extensibility.

The project follows clean architecture principles with a clear separation of concerns while remaining easy to deploy and maintain.
